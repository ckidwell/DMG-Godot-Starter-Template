namespace DMGStarterTemplate;

// Saved to the save file by NAME (as dictionary keys in AchievementData). Add new values
// anywhere, but never rename or remove one once a build has shipped: a name that no longer
// exists is skipped on load with a warning, and that player's unlock for it is lost.
public enum Achievements
{
    NONE,
    WELCOME_FIRST_TIME,
    DIED_FIRST_TIME,
    KILL_1,
}
