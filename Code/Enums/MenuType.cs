using Godot;
using System;

namespace DMGStarterTemplate;

public enum MenuType
{
    // Default value for "no menu shown yet"; never registered in _menuNodes.
    NONE,
    ACHIEVEMENTS,
    MAIN,
    PAUSE_QUIT,
    PLAY,
    SETTINGS,
    
    
}
